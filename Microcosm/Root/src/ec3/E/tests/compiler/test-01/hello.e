package ec.tests.hello;

/**
 * Very very basic compiler test; it's just hello-world, and
 * it's only Java. And, yes, it's Really Bad if the compiler
 * can't deal with it.
 */
public class Main
{
    public static void main (String args[]) {
        System.out.println("Hello, world!");
    }
}
